# HMAC secret for signing the demo bearer token. Replace before any deploy.
GLOBE_TOKEN_SECRET=globe-dev-secret-change-me

# Comma-separated list of allowed CORS origins.
ALLOWED_ORIGIN=http://localhost:8080

# SQLite file path (defaults to server/globe.db).
# GLOBE_DB_PATH=/var/data/globe.db

PORT=4000
