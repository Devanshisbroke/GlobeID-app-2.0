# Frontend → backend base URL.
# In Capacitor APK builds, set this to the public Cloudflare Tunnel URL
# (e.g. https://demo-globeid.trycloudflare.com/api/v1) and rebuild before
# `npx cap sync android`.
VITE_API_BASE_URL=http://localhost:4000/api/v1
